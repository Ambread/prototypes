select 0
write 1u32 2 // set length
write 3u32 2 // set io_end

write 0u32 0 // read all stdin
read 10u32   // push first char
eq '0'
jump_if #else

write 11u32 '\n' // add newline
write 0u32 1     // write all to stdout
halt

#else
write 0u32 1 // write all to stdout
jump #else
