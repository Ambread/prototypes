write 0 1u32 2 // set length
write 0 3u32 2 // set io_end

write 0 0u32 0 // read all stdin
read 0 10u32   // push first char
eq '0'
jump_if #else

write 0 11u32 '\n' // add newline
write 0 0u32 1     // write all to stdout
halt

#else
write 0 0u32 1 // write all to stdout
jump #else
